# ng-bootstrap-dropdown-prevent-offscreen
Angular directive for preventing bootstrap dropdown to appear off screen


1. Add as a dependency to your module.
2. Add directive to HTML-element with dropdown class
